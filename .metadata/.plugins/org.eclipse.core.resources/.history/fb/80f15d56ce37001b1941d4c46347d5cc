package com.cg.onlinehomedecor;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class OnlineHomeDecorApplication implements ApplicationRunner {

	public static void main(String[] args) {
		private static final Logger logger = LogManager.getLogger(OnlineHomeDecorApplication.class);

		 SpringApplication.run(OnlineHomeDecorApplication.class, args);
	}

	@Override
	public void run(ApplicationArguments args) throws Exception {
		
		logger.info();
	}

}
